<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Welcome to my world!</title>
  
  
  <link href="https://anyeyueren.gitee.io/atom.xml" rel="self"/>
  
  <link href="https://anyeyueren.gitee.io/"/>
  <updated>2021-04-01T14:32:54.255Z</updated>
  <id>https://anyeyueren.gitee.io/</id>
  
  <author>
    <name>anyeyueren</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Makefile学习笔记(二)</title>
    <link href="https://anyeyueren.gitee.io/2021/04/01/null/"/>
    <id>https://anyeyueren.gitee.io/2021/04/01/null/</id>
    <published>2021-04-01T13:49:35.000Z</published>
    <updated>2021-04-01T14:32:54.255Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\css\APlayer.min.css"><script src="\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\js\Meting.min.js"></script><h1 id="Makefile书写命令"><a href="#Makefile书写命令" class="headerlink" title="Makefile书写命令"></a>Makefile书写命令</h1><p>​    命令和操作系统 Shell 的命令行是一致的。make 会一按顺序一条一条的执行命令。</p><h2 id="显示命令"><a href="#显示命令" class="headerlink" title="显示命令"></a>显示命令</h2><ul><li>make在执行前会把要执行的命令输出到屏幕，但是**<u>用“@”字符在命令行之前，这个命令将不被 make 显示出来</u>**；</li><li>如果 make 执行时，带入 **<u>make 参数“-n”或“–just-print”</u>**，只是显示命令而不执行，这个功能都是用于调试makefile文件的；</li><li>**<u>make 参数“-s”或“–slient”则是全面禁止命令的显示</u>**；</li></ul><h2 id="命令执行"><a href="#命令执行" class="headerlink" title="命令执行"></a>命令执行</h2><p>​    当依赖目标新于目标时，也就是当规则的目标需要被更新时，make 会一条一条的执行其后的命令。</p><p>​    如果两条命令在执行时，第二条命令的执行需要在第一条命令执行完的基础上，那么<strong>需要用分号隔开这两条命令</strong>。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 如果没有分号，得不到想要的路径信息</span></span><br><span class="line"><span class="section">exec: </span></span><br><span class="line">cd /home/hchen; pwd</span><br></pre></td></tr></table></figure><h2 id="命令出错"><a href="#命令出错" class="headerlink" title="命令出错"></a>命令出错</h2><p>​    如果一个规则中的某个命令出错了（命令退出码非零），那么 make 就会终止执行当前规则，这将有可能终止所有规则的执行。有时并不是希望某个命令出错就终止。</p><p>​    忽略命令的出错，我们可以在 **<u>Makefile 的命令行前加一个减号“-”</u><strong>（在 Tab 键之后），标记为不管命令出不出错都认为是成功的。 另一个全局方法：</strong><u>给 make 加上“-i”或是“–ignore-errors”参数</u>**。</p><p>​    **<u>make 的参数的是“-k”或是“–keep-going”</u>**，这个参数的意思是，如果某规则中的命令出错了，那么就终目该规则的执行，但继续执行其它规则。</p><h2 id="嵌套执行make"><a href="#嵌套执行make" class="headerlink" title="嵌套执行make"></a>嵌套执行make</h2><p>​    某些大型项目模块众多，每个模块一个目录，每个目录都有自己模块的makefile，分开写有利于简洁明了，容易维护。而且又是不需要全编译，仅仅编译单个模块即可。此时就需要用到嵌套执行make功能。</p> <figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义$(MAKE)宏变量，当make需要一些参数时容易维护</span></span><br><span class="line"><span class="comment"># 这个 Makefile 叫做“总控 Makefile”</span></span><br><span class="line"><span class="section">subsystem: </span></span><br><span class="line"><span class="variable">$(MAKE)</span> -C subdir</span><br><span class="line"><span class="comment">#相当于</span></span><br><span class="line"><span class="section">subsystem: </span></span><br><span class="line">cd subdir &amp;&amp; <span class="variable">$(MAKE)</span></span><br></pre></td></tr></table></figure><p>​        总控 Makefile 的变量可以传递到下级的Makefile中（如果你显示的声明），但是不会覆盖下层的 Makefile中所定义的变量。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 如果你要传递变量到下级 Makefile 中，那么你可以使用这样的声明：</span></span><br><span class="line">   <span class="keyword">export</span> &lt;variable ...&gt;</span><br><span class="line"><span class="comment"># 如果你不想让某些变量传递到下级 Makefile 中，那么你可以这样声明:</span></span><br><span class="line">   <span class="keyword">unexport</span> &lt;variable ...&gt;</span><br><span class="line"><span class="comment"># 如果你要传递所有的变量，那么，只要一个 export 就行了,后面什么也不加</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 示例</span></span><br><span class="line"><span class="keyword">export</span> variable += value </span><br><span class="line"><span class="comment"># 其等价于： </span></span><br><span class="line">variable += value </span><br><span class="line"><span class="keyword">export</span> variable</span><br></pre></td></tr></table></figure><p>​    有两个变量，一个是 **<u>SHELL</u>**，一个是 **<u>MAKEFLAGS</u>**，这两个变量不管你是否 export，其总是要传递到下层 Makefile 中。 **<u>MAKEFLAGS</u>**变量中包含了make的参数信息。</p><h2 id="定义命令包"><a href="#定义命令包" class="headerlink" title="定义命令包"></a>定义命令包</h2><p>​    如果 Makefile 中出现一些相同命令序列，那么我们可以为这些相同的命令序列定义一个变量。定义这种命令序列的语法以“define”开始，以“endef”结束。例如：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># “run-yacc”是这个命令包的名字,中间的两行是命令序列</span></span><br><span class="line"><span class="comment"># 命令Yacc程序生成“y.tab.c”的文件，第二行的命令就是把这个文件改改名字</span></span><br><span class="line"><span class="keyword">define</span> run-yacc </span><br><span class="line">yacc <span class="variable">$(<span class="built_in">firstword</span> <span class="variable">$^</span>)</span> </span><br><span class="line">mv y.tab.c <span class="variable">$@</span> </span><br><span class="line"><span class="keyword">endef</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 在这个命令包使用中，命令包“run-yacc”中的“$^”就是“foo.y”，“$@”就是“foo.c”</span></span><br><span class="line">foo.c : foo.y </span><br><span class="line">$(run-yacc)</span><br></pre></td></tr></table></figure><h1 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h1><p>​    在 Makefile 中，变量可以使用在“目标”，“依赖目标”，“命令”或是 Makefile 的其它部分中。</p><ul><li>变量的命名字可以包含字符、数字，下划线（可以是数字开头）；</li><li>不应该含有“:”、“#”、“=”或是空字符（空格、回车等）；</li><li>变量是大小写敏感的；</li><li>变量在声明时需要给予初值，而在使用时，需要给在变量名前加上“$”符号，但最好用小括号“（）”或是大括号“{}”把变量给包括起来；</li><li>对于用真实的“$”字符，需要用“$$”来表示；</li></ul><h2 id="变量值替换"><a href="#变量值替换" class="headerlink" title="变量值替换"></a>变量值替换</h2><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#格式如下：</span></span><br><span class="line">$(var:a=b)  或者  $&#123;var:a=b&#125;</span><br><span class="line"><span class="comment"># 含义是： 把变量“var”中所有以“a”字串“结尾”的字符替换成“b”字串，“结尾”意思是“空格”或是“结束符”</span></span><br><span class="line"><span class="comment"># 举例如下：</span></span><br><span class="line"><span class="comment"># 先定义了一个“$(foo)”变量，第二行的意思是把“$(foo)”中所有以“.o”字串“结尾”全部替换成“.c”</span></span><br><span class="line"><span class="comment"># “$(bar)”的值就是“a.c b.c c.c&quot;</span></span><br><span class="line">foo := a.o b.o c.o </span><br><span class="line">bar := $(foo:.o=.c)   </span><br><span class="line"><span class="comment"># 或者使用静态模式定义 </span></span><br><span class="line">bar := $(foo:%.o=%.c)</span><br></pre></td></tr></table></figure><h2 id="变量值做变量"><a href="#变量值做变量" class="headerlink" title="变量值做变量"></a>变量值做变量</h2><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#案例一</span></span><br><span class="line"><span class="comment"># “$($($(z)))” 扩展为 “$($(y))”，进一步扩展为“$($(subst 1,2,$(x)))”</span></span><br><span class="line"><span class="comment"># $(x)的值是“variable1”，subst函数把“variable1”中的所有“1”字串替换成“2”字串，“variable1”变成“variable2”</span></span><br><span class="line"><span class="comment"># 最终$(a)的值就是$(variable2)的值——“Hello”</span></span><br><span class="line">x = variable1 </span><br><span class="line">variable2 := Hello </span><br><span class="line">y = <span class="variable">$(<span class="built_in">subst</span> 1,2,<span class="variable">$(x)</span>)</span> </span><br><span class="line">z = y </span><br><span class="line">a := $($(<span class="variable">$(z)</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment">#案例二</span></span><br><span class="line"><span class="comment"># 如果$(a1)的值是“a”的话，那么，$(sources)的值就是“a.c b.c c.c”</span></span><br><span class="line"><span class="comment"># 如果$(a1)的值是“1”，那么$(sources)的值是“1.c 2.c 3.c”</span></span><br><span class="line">a_objects := a.o b.o c.o </span><br><span class="line">1_objects := 1.o 2.o 3.o </span><br><span class="line">sources := $(<span class="variable">$(a1)</span>_objects:.o=.c)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 案例三</span></span><br><span class="line"><span class="comment"># 结合“函数”和“条件语句”</span></span><br><span class="line"><span class="comment"># 如果定义“do_sort”，那么foo := $(sort a d b g q c)，于是$(foo)的值就是“a b c d g q”</span></span><br><span class="line"><span class="comment"># 如果没有定义“do_sort”，那么foo := $(sort a d bg q c),即 调用strip函数</span></span><br><span class="line"><span class="keyword">ifdef</span> do_sort </span><br><span class="line">func := sort </span><br><span class="line"><span class="keyword">else</span> </span><br><span class="line">func := strip </span><br><span class="line"><span class="keyword">endif</span> </span><br><span class="line"> </span><br><span class="line">bar := a d b g q c </span><br><span class="line">foo := $(<span class="variable">$(func)</span> <span class="variable">$(bar)</span>)</span><br></pre></td></tr></table></figure><h2 id="追加变量值"><a href="#追加变量值" class="headerlink" title="追加变量值"></a>追加变量值</h2><p>​    可以使用“+=”操作符给变量追加值。</p><h2 id="override-指示符"><a href="#override-指示符" class="headerlink" title="override 指示符"></a>override 指示符</h2><p>​    有变量是通常 make的命令行参数设置的，那么 Makefile中对这个变量的赋值会被忽略。如果你想在 Makefile 中设置这类参数的值，那么，你可以使用“override”指示符。</p><h1 id="条件判断"><a href="#条件判断" class="headerlink" title="条件判断"></a>条件判断</h1><p>​        使用条件判断，可以让 make 根据运行时的不同情况选择不同的执行分支。条件表达式可以是<strong>比较变量的值</strong>，或是<strong>比较变量和常量的值</strong>。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法一：ifeq</span></span><br><span class="line"><span class="comment"># ifeq (&lt;arg1&gt;, &lt;arg2&gt;) </span></span><br><span class="line"><span class="comment"># ifeq意思表示条件语句的开始，并制定一个条件表达式</span></span><br><span class="line"><span class="comment"># else表示条件为假时情况，endif是条件语句结束</span></span><br><span class="line"><span class="keyword">ifeq</span>  条件判断</span><br><span class="line">     分支一</span><br><span class="line"><span class="keyword">else</span>  </span><br><span class="line">     分支二</span><br><span class="line"><span class="keyword">endif</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 比较参数“arg1”和“arg2”的值是否相同,相同为真</span></span><br><span class="line"><span class="keyword">ifeq</span> (&lt;arg1&gt;, &lt;arg2&gt;) </span><br><span class="line"><span class="keyword">ifeq</span> &#x27;&lt;arg1&gt;&#x27; &#x27;&lt;arg2&gt;&#x27; </span><br><span class="line"><span class="keyword">ifeq</span> <span class="string">&quot;&lt;arg1&gt;&quot;</span> <span class="string">&quot;&lt;arg2&gt;&quot;</span> </span><br><span class="line"><span class="keyword">ifeq</span> <span class="string">&quot;&lt;arg1&gt;&quot;</span> &#x27;&lt;arg2&gt;&#x27; </span><br><span class="line"><span class="keyword">ifeq</span> &#x27;&lt;arg1&gt;&#x27; <span class="string">&quot;&lt;arg2&gt;&quot;</span></span><br></pre></td></tr></table></figure><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法二：ifneq</span></span><br><span class="line"><span class="comment"># ifneq (&lt;arg1&gt;, &lt;arg2&gt;) </span></span><br><span class="line"><span class="comment"># 使用方法和ifeq相似，但是当“arg1”和“arg2”不同时为真</span></span><br></pre></td></tr></table></figure><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法三：ifdef</span></span><br><span class="line"><span class="comment"># ifdef &lt;variable-name&gt;</span></span><br><span class="line"><span class="comment"># 如果变量 &lt;variable-name&gt;的值非空，那到表达式为真。ifdef 只是测试一个变量是否有值</span></span><br></pre></td></tr></table></figure><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法四：ifndef</span></span><br><span class="line"><span class="comment"># ifndef &lt;variable-name&gt;</span></span><br><span class="line"><span class="comment"># 实际就是与ifdef相反</span></span><br></pre></td></tr></table></figure><h1 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h1><p>​    函数调用，很像变量的使用，也是以“$”来标识的，其语法如下：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># &lt;function&gt;就是函数名，make支持的函数不多</span></span><br><span class="line"><span class="comment"># &lt;arguments&gt;是函数的参数，参数间以逗号“,”分隔，而函数名和参数之间以“空格”分隔</span></span><br><span class="line"><span class="comment"># 函数调用以“$”开头，以圆括号或花括号把函数名和参数括起</span></span><br><span class="line">$(&lt;function&gt; &lt;arguments&gt;) </span><br><span class="line">或是 </span><br><span class="line">$&#123;&lt;function&gt; &lt;arguments&gt;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 案例</span></span><br><span class="line"><span class="comment"># $(comma)的值是一个逗号，$(space)使用了$(empty)定义了一个空格，，$(foo)的数值为“a b c”</span></span><br><span class="line"><span class="comment"># 调用了函数“subst”，这是一个替换函数，函数有三个参数</span></span><br><span class="line"><span class="comment"># 第一个参数是被替换字串，第二个参数是替换字串，第三个参数是替换操作作用的字串</span></span><br><span class="line"><span class="comment"># 这个函数也就是把$(foo)中的空格替换成逗号，所以$(bar)的值是“a,b,c”</span></span><br><span class="line">comma:= , </span><br><span class="line">empty:= </span><br><span class="line">space:= <span class="variable">$(empty)</span> <span class="variable">$(empty)</span> </span><br><span class="line">foo:= a b c </span><br><span class="line">bar:= <span class="variable">$(<span class="built_in">subst</span> <span class="variable">$(space)</span>,<span class="variable">$(comma)</span>,<span class="variable">$(foo)</span>)</span></span><br></pre></td></tr></table></figure><h2 id="字符串处理函数"><a href="#字符串处理函数" class="headerlink" title="字符串处理函数"></a>字符串处理函数</h2><h3 id="subst"><a href="#subst" class="headerlink" title="subst"></a>subst</h3><p><strong>$(subst from,to,text)</strong> </p><p>名称：字符串替换函数——subst。 </p><p>功能：把字串text中的from字符串替换成to。 </p><p>返回：函数返回被替换过后的字符串。 </p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 把“feet on the street”中的“ee”替换成“EE”，返回结果是“fEEt on the strEEt”</span></span><br><span class="line"><span class="variable">$(<span class="built_in">subst</span> ee,EE,feet on the street)</span></span><br></pre></td></tr></table></figure><h3 id="patsubst"><a href="#patsubst" class="headerlink" title="patsubst"></a>patsubst</h3><p><strong>$(patsubst pattern,replacement,text)</strong> </p><p>名称：模式字符串替换函数——patsubst。 </p><p>功能：查找text中的单词（单词以“空格”、“Tab”或“回车”“换行”分隔）是否符合模式pattern，如果匹配的话，则以replacement替换。这里，pattern可以包括通配符“%”，表示任意长度的字串。如果replacement中也包含“%”，那么，replacement中的这个“%”将是pattern中的那个“%”所代表的字串。（可以用“\”来转义，以“%”来表示真实含义的“%”字符） </p><p>返回：函数返回被替换过后的字符串。 </p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 把字串“x.c.c bar.c”符合模式[%.c]的单词替换成[%.o]，返回结果是“x.c.o bar.o”</span></span><br><span class="line"><span class="variable">$(<span class="built_in">patsubst</span> %.c,%.o,x.c.c bar.c)</span> </span><br><span class="line"></span><br><span class="line"><span class="section">“$(var:pattern=replacement)”    相当于   “$(patsubst pattern,replacement,<span class="variable">$(var)</span>)”， </span></span><br><span class="line"><span class="section">“$(var: suffix=replacement)”    相当于   “$(patsubst %suffix,%replacement,<span class="variable">$(var)</span>)”</span></span><br></pre></td></tr></table></figure><h3 id="strip"><a href="#strip" class="headerlink" title="strip"></a>strip</h3><p><strong>$(strip string)</strong> </p><p>名称：去空格函数——strip。 </p><p>功能：去掉string字串中开头和结尾的空字符。 </p><p>返回：返回被去掉空格的字符串值。 </p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 把字串“a b c ”去到开头和结尾的空格，结果是“a b c”</span></span><br><span class="line"><span class="variable">$(<span class="built_in">strip</span> a b c )</span></span><br></pre></td></tr></table></figure><h3 id="findstring"><a href="#findstring" class="headerlink" title="findstring"></a>findstring</h3><p><strong>$(findstring find,in)</strong> </p><p>名称：查找字符串函数——findstring。 </p><p>功能：在字串in中查找find字串。 </p><p>返回：如果找到，那么返回find，否则返回空字符串。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 第一个函数返回“a”字符串，第二个返回“”字符串（空字符串）</span></span><br><span class="line"><span class="variable">$(<span class="built_in">findstring</span> a,a b c)</span> </span><br><span class="line"><span class="variable">$(<span class="built_in">findstring</span> a,b c)</span></span><br></pre></td></tr></table></figure><h3 id="filter"><a href="#filter" class="headerlink" title="filter"></a>filter</h3><p><strong>$(filter pattern…,text)</strong> </p><p>名称：过滤函数——filter。 </p><p>功能：以pattern模式过滤text字符串中的单词，保留符合模式pattern的单词。可以有多个模式。 </p><p>返回：返回符合模式pattern的字串。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回的值是“foo.c bar.c baz.s”</span></span><br><span class="line">sources := foo.c bar.c baz.s ugh.h </span><br><span class="line"><span class="section">foo: <span class="variable">$(sources)</span> </span></span><br><span class="line">   cc <span class="variable">$(<span class="built_in">filter</span> %.c %.s,<span class="variable">$(sources)</span>)</span> -o foo</span><br><span class="line">   <span class="variable">$(<span class="built_in">filter</span> %.c %.s,<span class="variable">$(sources)</span>)</span></span><br></pre></td></tr></table></figure><h3 id="filter-out"><a href="#filter-out" class="headerlink" title="filter-out"></a><strong>filter-out</strong></h3><p><strong>$(filter-out pattern…,text)</strong> </p><p>名称：反过滤函数——filter-out。 </p><p>功能：以pattern模式过滤text字符串中的单词，去除符合模式pattern的单词。可以有多个模式。 </p><p>返回：返回不符合模式pattern的字串。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“foo.o bar.o”</span></span><br><span class="line">objects=main1.o foo.o main2.o bar.o </span><br><span class="line">mains=main1.o main2.o </span><br><span class="line"><span class="variable">$(<span class="built_in">filter</span>-out <span class="variable">$(mains)</span>,<span class="variable">$(objects)</span>)</span> </span><br></pre></td></tr></table></figure><h3 id="sort"><a href="#sort" class="headerlink" title="sort"></a>sort</h3><p><strong>$(sort list)</strong> </p><p>名称：排序函数——sort。 </p><p>功能：给字符串list中的单词排序（升序）。 </p><p>返回：返回排序后的字符串。 </p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回“bar foo lose”</span></span><br><span class="line">  <span class="variable">$(<span class="built_in">sort</span> foo bar lose)</span></span><br></pre></td></tr></table></figure><h3 id="word"><a href="#word" class="headerlink" title="word"></a>word</h3><p><strong>$(word n,text)</strong> </p><p>名称：取单词函数——word。 </p><p>功能：取字符串text中第n个单词。（从一开始） </p><p>返回：返回字符串text中第n个单词。如果n比text中的单词数要大，那么返 回空字符串。 </p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“bar”</span></span><br><span class="line"><span class="variable">$(<span class="built_in">word</span> 2, foo bar baz)</span></span><br></pre></td></tr></table></figure><h3 id="wordlist"><a href="#wordlist" class="headerlink" title="wordlist"></a><strong>wordlist</strong></h3><p><strong>$(wordlist s,e,text)</strong> </p><p>名称：取单词串函数——wordlist。 </p><p>功能：从字符串text中取从s开始到e的单词串。s和e是一个数字。 </p><p>返回：返回字符串text中从s到e的单词字串。如果s比text中的单词数要大，那么返回空字符串。如果e大于text的单词数，那么返回从s开始，到text结束的单词串。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“bar baz”</span></span><br><span class="line">  <span class="variable">$(<span class="built_in">wordlist</span> 2, 3, foo bar baz)</span></span><br></pre></td></tr></table></figure><h3 id="words"><a href="#words" class="headerlink" title="words"></a>words</h3><p><strong>$(words text)</strong> </p><p>名称：单词个数统计函数——words。 </p><p>功能：统计text中字符串中的单词个数。 </p><p>返回：返回text中的单词数。 </p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“3”</span></span><br><span class="line">$(words, foo bar baz)</span><br><span class="line"><span class="comment"># 取text中最后的一个单词</span></span><br><span class="line"><span class="variable">$(<span class="built_in">word</span> $(words text)</span>,text)</span><br></pre></td></tr></table></figure><h3 id="firstword"><a href="#firstword" class="headerlink" title="firstword"></a><strong>firstword</strong></h3><p><strong>$(firstword text)</strong> </p><p>名称：首单词函数——firstword。 </p><p>功能：取字符串text中的第一个单词。 </p><p>返回：返回字符串text的第一个单词。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“foo”,这个函数可以用 word 函数来实现：$(word 1,text)</span></span><br><span class="line"><span class="variable">$(<span class="built_in">firstword</span> foo bar)</span></span><br></pre></td></tr></table></figure><h2 id="文件名操作函数"><a href="#文件名操作函数" class="headerlink" title="文件名操作函数"></a><strong>文件名操作函数</strong></h2><h3 id="dir"><a href="#dir" class="headerlink" title="dir"></a>dir</h3><p><strong>$(dir names…)</strong> </p><p>名称：取目录函数——dir。 </p><p>功能：从文件名序列names中取出目录部分。目录部分是指最后一个反斜杠（“/”）之前的部分。如果没有反斜杠，那么返回“./”。</p><p>返回：返回文件名序列names的目录部分。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“src/ ./”</span></span><br><span class="line"> <span class="variable">$(<span class="built_in">dir</span> src/foo.c hacks)</span></span><br></pre></td></tr></table></figure><h3 id="notdir"><a href="#notdir" class="headerlink" title="notdir"></a>notdir</h3><p><strong>$(notdir names…)</strong></p><p>名称：取文件函数——notdir。 </p><p>功能：从文件名序列names中取出非目录部分。非目录部分是指最后一个反斜杠（“ /”）之后的部分。 </p><p>返回：返回文件名序列names的非目录部分。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“foo.c hacks”</span></span><br><span class="line"> <span class="variable">$(<span class="built_in">notdir</span> src/foo.c hacks)</span></span><br></pre></td></tr></table></figure><h3 id="suffix"><a href="#suffix" class="headerlink" title="suffix"></a><strong>suffix</strong></h3><p><strong>$(suffix names…)</strong> </p><p>名称：取后缀函数——suffix。 </p><p>功能：从文件名序列names中取出各个文件名的后缀。 </p><p>返回：返回文件名序列names的后缀序列，如果文件没有后缀，则返回空字串。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“.c .c”</span></span><br><span class="line">：<span class="variable">$(<span class="built_in">suffix</span> src/foo.c src-1.0/bar.c hacks)</span></span><br></pre></td></tr></table></figure><h3 id="basename"><a href="#basename" class="headerlink" title="basename"></a><strong>basename</strong></h3><p><strong>$(basename names…)</strong> </p><p>名称：取前缀函数——basename。 </p><p>功能：从文件名序列names中取出各个文件名的前缀部分。 </p><p>返回：返回文件名序列names的前缀序列，如果文件没有前缀，则返回空字串。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#  返回值是“src/foo src-1.0/bar hacks”</span></span><br><span class="line"><span class="variable">$(<span class="built_in">basename</span> src/foo.c src-1.0/bar.c hacks)</span></span><br></pre></td></tr></table></figure><h3 id="addsuffix"><a href="#addsuffix" class="headerlink" title="addsuffix"></a><strong>addsuffix</strong></h3><p><strong>$(addsuffix suffix,names…)</strong> </p><p>名称：加后缀函数——addsuffix。 </p><p>功能：把后缀suffix加到names中的每个单词后面。 </p><p>返回：返回加过后缀的文件名序列。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“foo.c bar.c”</span></span><br><span class="line"><span class="variable">$(<span class="built_in">addsuffix</span> .c,foo bar)</span></span><br></pre></td></tr></table></figure><h3 id="addprefix"><a href="#addprefix" class="headerlink" title="addprefix"></a><strong>addprefix</strong></h3><p><strong>$(addprefix prefix,names…)</strong> </p><p>名称：加前缀函数——addprefix。 </p><p>功能：把前缀prefix加到names中的每个单词后面。 </p><p>返回：返回加过前缀的文件名序列。 </p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“src/foo src/bar”</span></span><br><span class="line"><span class="variable">$(<span class="built_in">addprefix</span> src/,foo bar)</span></span><br></pre></td></tr></table></figure><h3 id="join"><a href="#join" class="headerlink" title="join"></a><strong>join</strong></h3><p><strong>$(join list1,list2)</strong> </p><p>名称：连接函数——join。 </p><p>功能：把list2中的单词对应地加到list1的单词后面。如果list1的单词个数要比list2的多，那么，list1中的多出来的单词将保持原样。如果list2的单词个数要比list1多，那么，list2多出来的单词将被复制到list2中。 </p><p>返回：返回连接过后的字符串。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 返回值是“aaa111 bbb222 333”</span></span><br><span class="line"><span class="variable">$(<span class="built_in">join</span> aaa bbb , 111 222 333)</span></span><br></pre></td></tr></table></figure><h2 id="if函数"><a href="#if函数" class="headerlink" title="if函数"></a>if函数</h2><p>​    if 函数很像 GNU 的 make 所支持的条件语句——ifeq。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#语法一</span></span><br><span class="line"><span class="variable">$(<span class="built_in">if</span> condition,then-part)</span></span><br><span class="line"><span class="comment">#语法二(if 函数可以包含“else”部分，或是不含)</span></span><br><span class="line"><span class="variable">$(<span class="built_in">if</span> condition,then-part,else-part)</span></span><br><span class="line"><span class="comment"># condition参数是 if 的表达式，如果其返回的为非空字符串，那么这个表达式then-part会被执行，否则else-part被执行</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 返回值</span></span><br><span class="line"><span class="comment"># 如果condition为真（非空字符串），那个then-part会是整个函数的返回值</span></span><br><span class="line"><span class="comment"># 如果condition为假（空字符串），  那么else-part会是整个函数的返回值</span></span><br><span class="line"><span class="comment"># 此时如果else-part没有被定义，那么，整个函数返回空字串</span></span><br></pre></td></tr></table></figure><h2 id="foreach函数"><a href="#foreach函数" class="headerlink" title="foreach函数"></a>foreach函数</h2><p>​    Makefile中的foreach函数几乎都是仿照于 Unix 标准 Shell（/bin/sh）中的 for 语句，这个函数是用来作为循环使用的。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法规则</span></span><br><span class="line"><span class="comment"># var最好是一个变量名，list可以是一个表达式</span></span><br><span class="line"><span class="variable">$(<span class="built_in">foreach</span> var,list,text)</span></span><br><span class="line"><span class="comment"># 把参数list中的单词逐一取出放到参数var所指定的变量中，然后再执行text所包含的表达式；</span></span><br><span class="line"><span class="comment"># 每一次text会返回一个字符串，循环过程中，text的所返回的每个字符串会以空格分隔;</span></span><br><span class="line"><span class="comment"># 最后当整个循环结束时，text所返回的每个字符串所组成的整个字符串（以空格分隔）将会是 foreach 函数的返回值</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 案例</span></span><br><span class="line"><span class="comment"># $(name)中的单词会被挨个取出，并存到变量“n”中，这些值以空格分隔，最后$(files)的值是“a.o b.o c.o d.o”</span></span><br><span class="line"><span class="comment"># 注意：foreach 中的var参数是一个临时的局部变量，foreach 函数执行完后，参数var变量不再作用</span></span><br><span class="line">names := a b c d </span><br><span class="line">files := <span class="variable">$(<span class="built_in">foreach</span> n,<span class="variable">$(names)</span>,<span class="variable">$(n)</span>.o)</span></span><br></pre></td></tr></table></figure><h2 id="call函数"><a href="#call函数" class="headerlink" title="call函数"></a>call函数</h2><p>​    call 函数是唯一一个可以用来创建新的参数化的函数。你可以写一个非常复杂的表达式，这个表达式中，你可以定义许多参数，然后你可以用 call 函数来向这个表达式传递参数。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 当 make 执行这个函数时，expression参数中的变量，如$(1)，$(2)，$(3)等，会被参数parm1，parm2，parm3依次取代。而expression的返回值就是 call 函数的返回值。</span></span><br><span class="line">  <span class="variable">$(<span class="built_in">call</span> expression,parm1,parm2,parm3...)</span></span><br><span class="line">  </span><br><span class="line"><span class="comment"># 案例</span></span><br><span class="line">reverse = $(1) $(2) </span><br><span class="line">foo = <span class="variable">$(<span class="built_in">call</span> reverse,a,b)</span> </span><br><span class="line"><span class="comment"># 那么，foo 的值就是“a b”。当然，参数的次序是可以自定义的，不一定是顺序的，如： </span></span><br><span class="line">reverse = $(2) $(1) </span><br><span class="line">foo = <span class="variable">$(<span class="built_in">call</span> reverse,a,b)</span> </span><br><span class="line"><span class="comment"># 此时的 foo 的值就是“b a”。</span></span><br></pre></td></tr></table></figure><h2 id="origin-函数"><a href="#origin-函数" class="headerlink" title="origin 函数"></a><strong>origin</strong> <strong>函数</strong></h2><p>​    origin 函数并不操作变量的值，只是告诉你你的这个变量是哪里的。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 语法</span></span><br><span class="line"><span class="comment"># Origin 函数会以其返回值来告诉你这个变量的“出生情况”</span></span><br><span class="line"><span class="comment"># 注意：variable是变量的名字，不应该是引用。所以你最好不要在variable中使用$</span></span><br><span class="line"><span class="variable">$(<span class="built_in">origin</span> variable)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 返回值</span></span><br><span class="line"><span class="comment"># “undefined” ==  如果variable从来没有定义过，origin 函数返回这个值“undefined”</span></span><br><span class="line"><span class="comment"># “default” == variable是一个默认的定义</span></span><br><span class="line"><span class="comment"># “file” == 如果variable这个变量被定义在 Makefile 中</span></span><br><span class="line"><span class="comment"># “command line” == 如果variable这个变量是被命令行定义的</span></span><br><span class="line"><span class="comment"># “override” == 如果variable是被 override 指示符重新定义的</span></span><br><span class="line"><span class="comment"># “automatic” == 如果variable是一个命令运行中的自动化变量</span></span><br></pre></td></tr></table></figure><h2 id="shell函数"><a href="#shell函数" class="headerlink" title="shell函数"></a>shell函数</h2><p>​    shell函数的参数应该就是操作系统 Shell 的命令，shell 函数把执行操作系统命令后的输出作为函数返回。可以用操作系统命令以及字符串处理命令 awk，sed 等等命令来生成一个变量。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 这个函数会新生成一个 Shell 程序来执行命令</span></span><br><span class="line">contents := <span class="variable">$(<span class="built_in">shell</span> cat foo)</span> </span><br><span class="line">files := <span class="variable">$(<span class="built_in">shell</span> echo *.c)</span></span><br></pre></td></tr></table></figure><h2 id="控制make函数"><a href="#控制make函数" class="headerlink" title="控制make函数"></a>控制make函数</h2><h3 id="error"><a href="#error" class="headerlink" title="error"></a>error</h3><p><strong>$(error text …)</strong> </p><p>产生一个致命的错误，text …是错误信息。注意，error 函数不会在一被使用就会产生错误信息，所以如果你把其定义在某个变量中，并在后续的脚本中使用这个变量，那么也是可以的。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在变量 ERROR_001 定义了后执行时产生 error 调用</span></span><br><span class="line"><span class="keyword">ifdef</span> ERROR_001 </span><br><span class="line"><span class="variable">$(<span class="built_in">error</span> <span class="built_in">error</span> is <span class="variable">$(ERROR_001)</span>)</span> </span><br><span class="line"><span class="keyword">endif</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 在目录 err被执行时才发生 error 调用</span></span><br><span class="line">ERR = <span class="variable">$(<span class="built_in">error</span> found an <span class="built_in">error</span>!)</span> </span><br><span class="line"><span class="meta"><span class="meta-keyword">.PHONY</span>: err </span></span><br><span class="line"><span class="section">err: ; <span class="variable">$(ERR)</span></span></span><br></pre></td></tr></table></figure><h3 id="warning"><a href="#warning" class="headerlink" title="warning"></a>warning</h3><p>$(warning text …) </p><p>这个函数很像 error 函数，只是它并不会让 make 退出，只是输出一段警告信息，而 make 继续执行。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;link rel=&quot;stylesheet&quot; class=&quot;aplayer-secondary-style-marker&quot; href=&quot;\css\APlayer.min.css&quot;&gt;&lt;script src=&quot;\js\APlayer.min.js&quot; class=&quot;aplayer-se</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>Makefile学习笔记(一)</title>
    <link href="https://anyeyueren.gitee.io/2021/03/23/null/"/>
    <id>https://anyeyueren.gitee.io/2021/03/23/null/</id>
    <published>2021-03-23T15:00:44.000Z</published>
    <updated>2021-03-28T14:34:57.731Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\css\APlayer.min.css"><script src="\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><script class="meting-secondary-script-marker" src="\js\Meting.min.js"></script><h1 id="预备知识"><a href="#预备知识" class="headerlink" title="预备知识"></a>预备知识</h1><p>Makefile文件中内容关系到整个工程的编译，内部定义了一系列的规则决定哪些文件需要先编译，哪些文件后编译，跟shell脚本类似。在学习makefile之前需要先简单了解下编译链接的原理。</p><h2 id="程序的编译和链接"><a href="#程序的编译和链接" class="headerlink" title="程序的编译和链接"></a>程序的编译和链接</h2><p>无论是C、C++语言都需要将源文件编译成中间文件，这个动作叫做编译(compile)，对于window下面这个中间文件就是.obj文件，在Unix下面就是.o文件。然后 再将.o文件链接成可执行文件，这个动作就是链接(link)。</p><img src="Makefile学习笔记.assets/wps3.jpg" style="zoom:100%;" /><h2 id="gcc编译器"><a href="#gcc编译器" class="headerlink" title="gcc编译器"></a>gcc编译器</h2><p>GNU CC(通常称为GCC)是GNU项目的编译器，它能够编译C、C++语言编写的程序。gcc是Linux开发的基础，gcc能够支持多种不同的C语言版本，同时提供Linux、Unix、Windows下的编译版本。gcc可以让跨平台编程更加容易，降低代码移植代价。</p><p>gcc编译C程序一般都是四个步骤：预处理、编译、汇编和链接。下面将介绍gcc常用的选项。</p><h3 id="gcc常用选项"><a href="#gcc常用选项" class="headerlink" title="gcc常用选项"></a>gcc常用选项</h3><table><thead><tr><th>选项</th><th align="left">含义</th></tr></thead><tbody><tr><td>-c</td><td align="left">只编译、汇编，不链接</td></tr><tr><td>-C</td><td align="left">预处理时保留注释信息</td></tr><tr><td>-E</td><td align="left">只进行预处理，不编译</td></tr><tr><td>-g</td><td align="left">包含调试信息</td></tr><tr><td>-I（大写i）</td><td align="left">指定include包含文件的搜索目录</td></tr><tr><td>-l</td><td align="left">链接指定的库文件</td></tr><tr><td>-o [file1] [file2]</td><td align="left">将文件 file2 编译成可执行文件 file1，如果不指定文件名则默认输出为a.out</td></tr><tr><td>-O[0,1,2,3]</td><td align="left">优化程序，默认2级优化，0 不优化，优化和调试不兼容，所以不要同时使用-g 和-O 选项。</td></tr><tr><td>-S</td><td align="left">只编译，不汇编</td></tr></tbody></table><h3 id="gcc高级选项"><a href="#gcc高级选项" class="headerlink" title="gcc高级选项"></a>gcc高级选项</h3><table><thead><tr><th>选项</th><th>详细</th></tr></thead><tbody><tr><td>-v</td><td>详细输出编译过程中所采用的每一个选项</td></tr><tr><td>-C</td><td>预处理时保留注释信息</td></tr><tr><td>-w</td><td>忽略所有告警</td></tr><tr><td>-Wall</td><td>开启所有告警信息</td></tr><tr><td>-g</td><td>可生成能被 gdb 调试器所使用的调试信息</td></tr><tr><td>-ggdb</td><td>产生更多的调试信息</td></tr></tbody></table><p>案例一：gcc编译文件不携带任何参数，生成可执行文件a.out</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">[root@centos7 Test]# gcc hello.c</span><br><span class="line">[root@centos7 Test]# ls -al</span><br><span class="line">-rwxr-xr-x. 1 root root 8360 Mar 27 05:40 a.out</span><br><span class="line">-rw-r--r--. 1 root root   78 Mar 27 05:39 hello.c</span><br><span class="line">[root@centos7 Test]# ./a.out </span><br><span class="line">Welcome to my world!</span><br></pre></td></tr></table></figure><p>案例二：linux可执行程序的扩展名不是exe，很多时候不需要扩展名，但是可以用hello.exe作为目标文件</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">[root@centos7 Test]# gcc -o hello hello.c</span><br><span class="line">[root@centos7 Test]# ls -al</span><br><span class="line">-rwxr-xr-x. 1 root root 8360 Mar 27 07:05 hello</span><br><span class="line">-rw-r--r--. 1 root root   78 Mar 27 05:39 hello.c</span><br><span class="line">[root@centos7 Test]# ./hello </span><br><span class="line">Welcome to my world!</span><br></pre></td></tr></table></figure><p>案例三：完整演示预编译、编译和链接的过程</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">//只进行预处理,不进行编译</span><br><span class="line">[root@centos7 Test]# gcc  -o   hello1.c  -E   hello.c</span><br><span class="line">[root@centos7 Test]# ls -al</span><br><span class="line">-rw-r--r--. 1 root root 16875 Mar 27 22:25 hello1.c   //预处理得到的文件</span><br><span class="line">-rw-r--r--. 1 root root    78 Mar 27 05:39 hello.c</span><br><span class="line">//编译</span><br><span class="line">[root@centos7 Test]# gcc -o hello.o -c hello1.c</span><br><span class="line">[root@centos7 Test]# ls -al</span><br><span class="line">-rw-r--r--. 1 root root 16875 Mar 27 23:15 hello1.c</span><br><span class="line">-rw-r--r--. 1 root root    78 Mar 27 05:39 hello.c</span><br><span class="line">-rw-r--r--. 1 root root  1504 Mar 27 23:16 hello.o</span><br><span class="line">//链接</span><br><span class="line">[root@centos7 Test]# gcc -o hello hello.o</span><br><span class="line">[root@centos7 Test]# ls -al</span><br><span class="line">-rwxr-xr-x. 1 root root  8360 Mar 27 23:16 hello</span><br><span class="line">-rw-r--r--. 1 root root 16875 Mar 27 23:15 hello1.c</span><br><span class="line">-rw-r--r--. 1 root root    78 Mar 27 05:39 hello.c</span><br><span class="line">-rw-r--r--. 1 root root  1504 Mar 27 23:16 hello.o</span><br></pre></td></tr></table></figure><p>对于.c文件gcc就会用c语言的规范去编译，.cpp文件gcc就会用c++语言编译。另外g++是gcc的C++编译器，建议编译c++的文件使用g++编译器。</p><hr><h1 id="Makefile介绍"><a href="#Makefile介绍" class="headerlink" title="Makefile介绍"></a>Makefile介绍</h1><p>​    在执行make命令时需要一个makefile文件，文件中的语法指定make执行时如何进行编译链接。make是一种控制编译或者重复编译软件的工具。make可以自动管理软件的编译内容、方式和时机，从而使程序员把更多的精力集中在编写代码上。</p><p>​    makefile是一个文本形式的脚本文件，其中包含一些规则告诉make编译哪些文件，怎么样编译以及在什么条件下编译。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#makefile规则遵循以下通用形式</span></span><br><span class="line"><span class="comment">#每个command第一个字符必须是tab键，而不是空格键，不然make会报错并停止</span></span><br><span class="line"><span class="comment">#target:也就是一个目标文件，可以是目标文件，也可以是执行文件,还可以是一个标签</span></span><br><span class="line"><span class="comment">#dependency:就是生成target所需要的文件或者目标</span></span><br><span class="line"><span class="comment">#command:就是make需要执行的命令</span></span><br><span class="line"><span class="section">target:dependency [dependency[…]]</span></span><br><span class="line">command</span><br><span class="line">command</span><br><span class="line">[…]</span><br><span class="line"><span class="comment">#或者</span></span><br><span class="line"><span class="comment"># command 是命令行，如果其不与“target:prerequisites”在一行，那么，必须以[Tab键]开头，如果和 prerequisites 在一行，那么可以用分号做为分隔</span></span><br><span class="line"><span class="comment">#如果命令太长，你可以使用反斜框（‘\’）作为换行符</span></span><br><span class="line">targets : prerequisites ; command </span><br><span class="line">command</span><br><span class="line">[…]</span><br></pre></td></tr></table></figure><p>​    Makefile核心内容：target中一个或多个目标文件依赖于dependency中的各个文件，生成规则在command中定义。一旦依赖文件有更新那么在make时command所定义的命令就会被执行。</p><p>可以看下面这个案例：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">//生成可执行文件edit依赖三个.o文件，而三个.o文件依赖于各自的.c和.h文件</span><br><span class="line">//脚本内容存放在makefile文件中，当同级目录下执行make即可生成目标文件edit</span><br><span class="line">//如果想要删除中间目标文件只需要执行make clean即可</span><br><span class="line"></span><br><span class="line">edit : main.o kbd.o command.o display.o</span><br><span class="line">gcc -o edit main.o kbd.o command.o display.o</span><br><span class="line"></span><br><span class="line">main.o : main.c defs.h </span><br><span class="line">gcc -c main.c </span><br><span class="line">kbd.o : kbd.c defs.h command.h </span><br><span class="line">gcc -c kbd.c </span><br><span class="line">command.o : command.c defs.h command.h </span><br><span class="line">gcc -c command.c </span><br><span class="line">display.o : display.c defs.h buffer.h </span><br><span class="line">gcc -c display.c</span><br><span class="line"></span><br><span class="line">clean : </span><br><span class="line">rm edit main.o kbd.o command.o display.o</span><br></pre></td></tr></table></figure><h2 id="make工作机制"><a href="#make工作机制" class="headerlink" title="make工作机制"></a>make工作机制</h2><p>​    一般在目录下输入make命令即可执行当前目录下的makefile脚本：</p><ol><li>执行make时会在当前目录中寻找makefile文件；</li><li>搜索到makefile脚本之后，会寻找最终需要生成的目标文件；</li><li>当目标文件不存在或者目标文件所依赖的.o文件修改有更新，则会继续向下执行下面的命令。整个make的依赖性，make会一层一层去找文件的依赖关系，直到最终编译；</li></ol><p>​    在找寻的过程中，如果出现错误，比如最后被依赖的文件找不到，那么make 就会直接退出，并报错，而对于所定义的命令的错误，或是编译不成功，make 根本不理。</p><h2 id="makefile中使用变量"><a href="#makefile中使用变量" class="headerlink" title="makefile中使用变量"></a>makefile中使用变量</h2><p>​    上面的案例中几个.o文件被使用多次，如果此时工程中需要添加新的.o文件就需要每一处挨着添加，所以此时可以声明一个变量，类似于C语言中的宏定义，叫objects，OBJECTS，objs，OBJS，obj，或是OBJ。那么可以在文件开始的地方这样定义：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">objects = main.o kbd.o command.o display.o</span><br><span class="line"></span><br><span class="line">edit : <span class="variable">$(objects)</span></span><br><span class="line">gcc -o edit <span class="variable">$(objects)</span></span><br><span class="line"></span><br><span class="line">main.o : main.c defs.h </span><br><span class="line">gcc -c main.c </span><br><span class="line">kbd.o : kbd.c defs.h command.h </span><br><span class="line">gcc -c kbd.c </span><br><span class="line">command.o : command.c defs.h command.h </span><br><span class="line">gcc -c command.c </span><br><span class="line">display.o : display.c defs.h buffer.h </span><br><span class="line">gcc -c display.c</span><br><span class="line">clean : </span><br><span class="line">rm edit <span class="variable">$(objects)</span></span><br></pre></td></tr></table></figure><h2 id="make自动推导"><a href="#make自动推导" class="headerlink" title="make自动推导"></a>make自动推导</h2><p>make可以自动推导文件以及文件依赖关系后面的命令，这样就没有必要在每个.o文件后面写上类似的命令。只要make看到一个[.o]文件就会自动把[.c]文件加在依赖关系中。例如：make找到了main.o，那么main.c就会是main.o的依赖文件，并且gcc  -c  main.c也会被推导出来。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">objects = main.o kbd.o command.o display.o</span><br><span class="line"></span><br><span class="line">edit : <span class="variable">$(objects)</span></span><br><span class="line">gcc -o edit <span class="variable">$(objects)</span></span><br><span class="line"></span><br><span class="line">main.o : defs.h </span><br><span class="line">kbd.o :  defs.h command.h </span><br><span class="line">command.o :  defs.h command.h </span><br><span class="line">display.o : defs.h buffer.h </span><br><span class="line">clean : </span><br><span class="line">rm edit <span class="variable">$(objects)</span></span><br></pre></td></tr></table></figure><p>​    既然提供了自动推导命令和文件功能，那么上面的.o和.h的依赖也能够收起来。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">objects = main.o kbd.o command.o display.o</span><br><span class="line"></span><br><span class="line">edit : <span class="variable">$(objects)</span></span><br><span class="line">gcc -o edit <span class="variable">$(objects)</span></span><br><span class="line"></span><br><span class="line"><span class="variable">$(objects)</span> : defs.h     <span class="comment">#所以.o都依赖defs.h</span></span><br><span class="line">kbd.o command.o : command.h</span><br><span class="line">display.o : buffer.h </span><br><span class="line">clean : </span><br><span class="line">rm edit <span class="variable">$(objects)</span></span><br></pre></td></tr></table></figure><h2 id="清空目标文件规则"><a href="#清空目标文件规则" class="headerlink" title="清空目标文件规则"></a>清空目标文件规则</h2><p>​    每个 Makefile 中都应该写一个清空目标文件（.o 和执行文件）的规则。</p><ul><li>.PHONY 意思表示 clean 是一个“伪目标”；</li><li>在 rm 命令前面加了一个小减号的意思就是，也许某些文件出现问题，但不要管，继续做后面的事；</li><li>clean 的规则不要放在文件的开头，不然，这就会变成 make 的默认目标；</li></ul><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">clean: </span></span><br><span class="line">rm edit <span class="variable">$(objects)</span></span><br><span class="line"></span><br><span class="line">更改为：</span><br><span class="line">.PHONY : clean </span><br><span class="line">clean : </span><br><span class="line">-rm edit <span class="variable">$(objects)</span></span><br></pre></td></tr></table></figure><h2 id="Makefile文件之间引用"><a href="#Makefile文件之间引用" class="headerlink" title="Makefile文件之间引用"></a>Makefile文件之间引用</h2><p>​    在Makefile使用 <code>include</code> 关键字可以把别的Makefile包含进来，被包含的文件会原模原样的放在当前文件的包含位置。<code>include</code>的语法是：include  <filename> 。注意在 <code>include</code>前面可以有一些空字符，但是绝不能是[Tab]键开始。include和filename之间可以用空格隔开。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#举例：现存在a.mk,b.mk,c.mk、foo.make的文件，以及一个变量$(bar),变量中e.mk和f.mk</span></span><br><span class="line">  <span class="keyword">include</span> foo.make *.mk <span class="variable">$(bar)</span></span><br><span class="line">  <span class="comment">#相当于</span></span><br><span class="line">  <span class="keyword">include</span> foo.make a.mk b.mk c.mk e.mk f.mk</span><br></pre></td></tr></table></figure><p>如果文件都没有指定绝对路径或是相对路径的话，make 会在当前目录下首先寻找，如果当前目录下没有找到，那么，make 还会在下面的几个目录下找： </p><ul><li>若make执行时有“-I”或者“–include-dir”参数，那么make就会在这个参数所指定的目录下寻找；</li><li>如果目录<prefix>/include（一般是：/usr/local/bin 或/usr/include）存在的话，make 也会去找；</li></ul><p><strong>若未找到include所包含的文件：</strong></p><p>​    如果有文件没有找到的话，make 会生成一条警告信息，但不会马上出现致命错误。它会继续载入其它的文件，一旦完成 makefile 的读取，make 会再重试这些没有找到或者不能读取的文件。</p><p>​    如果还是不行，make才会出现一条致命信息！如果不想理会这些无法读取的文件，让make操作继续往下执行，那么可以在include前面加一个减号“-“。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#其表示，无论 include 过程中出现什么错误，都不要报错继续执行。和其它版本 make兼 容的相关命令是 sinclude，其作用和这一个是一样的。</span></span><br><span class="line"> <span class="keyword">-include</span> &lt;filename&gt;</span><br></pre></td></tr></table></figure><h2 id="环境变量MAKEFILES"><a href="#环境变量MAKEFILES" class="headerlink" title="环境变量MAKEFILES"></a>环境变量MAKEFILES</h2><p>当前环境中定义了环境变量 MAKEFILES，那么，make 会把这个变量中的值做一个类似于 include 的动作。这个变量中的值是其它的 Makefile，用空格分隔。只是，它和 include不同的是，从这个环境变中引入的 Makefile 的“目标”不会起作用，如果环境变量中定义的文件发现错误，make 也会不理。 </p><p>但是因为一旦变量被定义出来那么所有的makefile都会受到它的影响，故不建议使用。</p><hr><h1 id="Makefile综述"><a href="#Makefile综述" class="headerlink" title="Makefile综述"></a>Makefile综述</h1><p>​    Makefile 里主要包含了五个东西：显式规则、隐晦规则、变量定义、文件指示和注释。</p><ul><li><strong>显示规则</strong>：即显示的指出要生成的文件，文件的依赖文件，生成命令；</li><li><strong>隐晦规则</strong>：即之前的make的自动推导功能，隐晦的规则能简化书写makefile;</li><li><strong>变量定义</strong>：当 Makefile 被执行时，其中的变量都会被扩展到相应的引用位置上；</li><li><strong>文件指示</strong>：一是在某个Makefile中引用另外一个makefile；二是根据某些情况制定makefile中的有效部分，相当于C语言中的#if；三是定义一个多行命令。</li><li><strong>注释</strong>：Makefile中只有行注释，其注释用“#“字符，如果想在makefile中使用”#”只需要用转义字符，如”#“。</li></ul><p>另外：在 Makefile 中的命令，必须要以<u>[Tab]</u>键开始。</p><h2 id="make执行步骤"><a href="#make执行步骤" class="headerlink" title="make执行步骤"></a>make执行步骤</h2><p>​    make工作时的执行步骤如下：</p><ol><li> 读入所有的makefile文件；</li><li>读入被include的其他makefile文件；</li><li>初始化文件中的变量；</li><li>推导隐晦规则，并分析所有规则；</li><li>为所有的目标文件创建依赖关系链；</li><li>根据依赖关系，决定哪些目标要重新生成；</li><li>执行生成命令；</li></ol><h2 id="make书写规则"><a href="#make书写规则" class="headerlink" title="make书写规则"></a>make书写规则</h2><p>​    规则包含两个部分，一个是依赖关系，一个是生成目标的方法。</p><p>​    在makefile中<strong>只应该有一个最终目标文件</strong>，其他的目标文件都是被带出来的，所以规则的顺序很重要。回看makefile工作机制中代码行注释。一般来说，make 会以 UNIX 的标准 Shell，也就是/bin/sh 来执行命令。</p><h3 id="通配符"><a href="#通配符" class="headerlink" title="通配符"></a>通配符</h3><p>​    make中可以使用一些通配符来定义一系列类似的文件，**<u>make支持三种通配符：“*”，“?”，“[…]“</u>*<em>。例如：“</em>.c”表示所以后缀为 c 的文件，*如果我们的文件名中有通配符，如：“”，那么可以用转义字符“\”。</p><h3 id="文件搜索"><a href="#文件搜索" class="headerlink" title="文件搜索"></a>文件搜索</h3><p>​    一个工程中源件众多时，源文件被分配存放在不同目录下，当make需要去寻找文件的依赖关系时，可以在文件前加上路径名，但是最好的方法是把一个路径告知make，make自动寻找依赖文件。</p><p>​    Makefile文件中的特殊变量“**<u>VPATH</u>**”便是完成此项功能(<u>如果没有指明该变量make只会在当前的目录中寻找依赖文件和目标文件</u>）。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">##设置方法一：</span></span><br><span class="line"><span class="comment">#如果定义了这个变量，那么，make就会在当当前目录找不到的情况下，到所指定的目录中去找寻文件了</span></span><br><span class="line">VPATH = src:../headers</span><br><span class="line"><span class="comment">#上面的的定义指定两个目录，“src”和“../headers”，make 会按照这个顺序进行搜索。目录由“冒号”分隔</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">##设置方法二：</span></span><br><span class="line"><span class="comment">#另一个设置文件搜索路径的方法是使用 make 的“vpath”关键字(注意小写)，它可以指定不同的文件在不同的搜索目录中</span></span><br><span class="line">  <span class="comment">#方式一：为符合模式&lt;pattern&gt;的文件指定搜索目录&lt;directories&gt;</span></span><br><span class="line">     <span class="keyword">vpath</span> &lt;pattern&gt; &lt;directories&gt;</span><br><span class="line">     <span class="comment">## 该方式中的&lt;pattern&gt;需要包含“%”字符。“%”的意思是匹配零或若干字符，“%.h”表示的就是以“.h”结尾的文件</span></span><br><span class="line">     <span class="comment">## &lt;pattern&gt;指定了要搜索的文件集，&lt;directories&gt;则指定了&lt;pattern&gt;的文件集的搜索的目录</span></span><br><span class="line">     </span><br><span class="line">  <span class="comment">#方式二：清除符合模式&lt;pattern&gt;的文件的搜索目录</span></span><br><span class="line">     <span class="keyword">vpath</span> &lt;pattern&gt;</span><br><span class="line">  <span class="comment">#方式三：清除所有已被设置好了的文件搜索目录</span></span><br><span class="line">     <span class="keyword">vpath</span></span><br></pre></td></tr></table></figure><p>实际案例分析：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 案例一：该语句表示，要求 make 在“../headers”目录下搜索所有以“.h”结尾的文件(当然默认会在当前目录下先寻找)</span></span><br><span class="line">    <span class="keyword">vpath</span> %.h ../headers</span><br><span class="line"><span class="comment"># 案例二：连续使用vpath语句指定不同搜索策略，其表示“.c”结尾的文件，先在“foo”目录，然后是“blish”，最后是“bar”目录</span></span><br><span class="line">    <span class="keyword">vpath</span> %.c foo </span><br><span class="line">    <span class="keyword">vpath</span> % blish </span><br><span class="line">    <span class="keyword">vpath</span> %.c bar</span><br><span class="line"><span class="comment"># 案例三：下面语句则表示“.c”结尾的文件，先在“foo”目录，然后是“bar”目录，最后才是“blish”目录</span></span><br><span class="line">    <span class="keyword">vpath</span> %.c foo:bar </span><br><span class="line">    <span class="keyword">vpath</span> % blish</span><br></pre></td></tr></table></figure><h3 id="伪目标"><a href="#伪目标" class="headerlink" title="伪目标"></a>伪目标</h3><p>​    clean操作用来清除所有中间文件，但是clean并不是一个目标文件，不会生成一个名为“clean”的文件。“伪目标”并不是一个文件，只是一个标签，所以在make时无法确定clean对应的命令是否要执行，那么只能通过显示地方式指明这个“目标”才能让其生效。</p><p>​    <strong>“伪目标”的取名不能和文件名重名，不然其就失去了“伪目标”的意义了！</strong>为了避免和文件重名的这种情况，我们可以使用一个特殊的标记“.PHONY”来显示地指明一个目标是“伪目标”，向 make 说明，不管是否有这个文件，这个目标就是“伪目标”</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">.PHONY : clean</span><br><span class="line"></span><br><span class="line"><span class="comment">#整个过程可以如此写：</span></span><br><span class="line"><span class="meta"><span class="meta-keyword">.PHONY</span>: clean </span></span><br><span class="line"><span class="section">clean: </span></span><br><span class="line">rm *.o temp</span><br></pre></td></tr></table></figure><p>​    伪目标也可以作为“默认目标“，这时需要将其放在第一个。如果你的 Makefile 需要一口气生成若干个可执行文件，但你只想简单地敲一个 make 完事，可以使用下面的方法：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Makefile中第一个目标会被作为默认目标，声明了一个all伪目标，其依赖其他三个目标</span></span><br><span class="line"><span class="comment"># 伪目标的特性是总是被执行的，那么all依赖的三个文件就不如all目标新，那么三个目标总是被决议，达到了一口气生成多个目标目的</span></span><br><span class="line">all : prog1 prog2 prog3 </span><br><span class="line">.PHONY : all </span><br><span class="line"> </span><br><span class="line">prog1 : prog1.o utils.o </span><br><span class="line">gcc -o prog1 prog1.o utils.o </span><br><span class="line"> </span><br><span class="line">prog2 : prog2.o </span><br><span class="line">gcc -o prog2 prog2.o </span><br><span class="line"> </span><br><span class="line">prog3 : prog3.o sort.o utils.o </span><br><span class="line">gcc -o prog3 prog3.o sort.o utils.o</span><br></pre></td></tr></table></figure><p>​    伪目标也可以成为依赖：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># “cleanall”、“cleanobj”、“cleandiff”三者都是伪目标，可以执行make cleanall、make cleanobj和make cleandiff命令来达到不同的清除目的</span></span><br><span class="line"><span class="meta"><span class="meta-keyword">.PHONY</span>: cleanall cleanobj cleandiff </span></span><br><span class="line"> </span><br><span class="line">cleanall : cleanobj cleandiff </span><br><span class="line">rm program </span><br><span class="line">cleanobj : </span><br><span class="line">rm *.o </span><br><span class="line">cleandiff : </span><br><span class="line">rm *.diff</span><br></pre></td></tr></table></figure><h3 id="多目标"><a href="#多目标" class="headerlink" title="多目标"></a>多目标</h3><p>​    Makefile 的规则中的目标可以不止一个，其支持多目标，有可能我们的多个目标同时依赖于一个文件，并且其生成的命令大体类似。下面的脚本中生成了两个目标文件，两个目标文件都依赖于同一个名字的文件text.g，且text.g的生成语法相似！</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">bigoutput : text.g </span><br><span class="line">generate text.g -big &gt; bigoutput </span><br><span class="line">littleoutput : text.g </span><br><span class="line">generate text.g -little &gt; littleoutput</span><br></pre></td></tr></table></figure><p>此时可以将其进行合并处理，多个目标的生成规则执行命令是同一个，这时需要使用一个**<u>自动化变量”$@”</u>**。这个变量表示着目前规则中所有的目标的集合。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># “-$(subst output,,$@)”中的“$”表示执行一个Makefile的函数，函数名为subst，后面为参数</span></span><br><span class="line"><span class="comment"># 函数subst是截取字符串的意思，“$@”表示目标的集合，就像一个数组</span></span><br><span class="line"><span class="comment"># “$@”依次取出目标，并执于命令</span></span><br><span class="line">bigoutput littleoutput : text.g </span><br><span class="line">generate text.g -<span class="variable">$(<span class="built_in">subst</span> output,,<span class="variable">$@</span>)</span> &gt; <span class="variable">$@</span></span><br></pre></td></tr></table></figure><h3 id="静态模式"><a href="#静态模式" class="headerlink" title="静态模式"></a>静态模式</h3><p>​    在静态模式中可以更加容易定义多目标的规则，下面是其语法：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># targets定义了一系列的目标文件，可以有通配符,是目标的集合；</span></span><br><span class="line"><span class="comment"># target-parrtern 是指明了targets 的模式，也就是的目标集模式；</span></span><br><span class="line"><span class="comment"># prereq-parrterns 是目标的依赖模式，它对 target-parrtern 形成的模式再进行一次依赖目标的定义；</span></span><br><span class="line">&lt;targets ...&gt;: &lt;target-pattern&gt;: &lt;prereq-patterns ...&gt; </span><br><span class="line">&lt;commands&gt; </span><br><span class="line">....</span><br></pre></td></tr></table></figure><p>举例理解该问题，现将<target-parrtern>定义成“%.o”，意思是我们的<target>集合中都是以“.o”结尾的文件，<prereq-parrterns>定义成“%.c”，意思是对<target-parrtern>所形成的目标集进行二次定义。</p><p>命令执行方法：取<target-parrtern>模式中的“%”（也就是去掉了[.o]这个结尾），并为其加上[.c]这个结尾，形成的新集合。所以无论是“目标模式“还是”依赖模式“都应该有”%“这个字符（若文件名中有%记得使用转义字符”&quot;进行转义）。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 案例中指出目标从$(objects)中获取</span></span><br><span class="line"><span class="comment"># “%.o”表明要所有以“.o”结尾的目标，也就是“foo.o bar.o”，也就是变量$object 集合的模式</span></span><br><span class="line"><span class="comment"># 依赖模式“%.c”则取模式“%.o”的“%”，也就是“foo bar”，并为其加下“.c”的后缀，于是依赖目标就是“foo.c bar.c”</span></span><br><span class="line"><span class="comment">#命令中的“$&lt;”和“$@”则是自动化变量</span></span><br><span class="line">objects = foo.o bar.o </span><br><span class="line"> </span><br><span class="line"><span class="section">all: <span class="variable">$(objects)</span> </span></span><br><span class="line"> </span><br><span class="line"><span class="variable">$(objects)</span>: %.o: %.c </span><br><span class="line">gcc -c <span class="variable">$(CFLAGS)</span> <span class="variable">$&lt;</span> -o <span class="variable">$@</span></span><br></pre></td></tr></table></figure><table><thead><tr><th>变量名</th><th>含义</th></tr></thead><tbody><tr><td>$@</td><td>“$@”表示目标集（也就是“foo.o bar.o”）</td></tr><tr><td>$&lt;</td><td>“$&lt;”表示所有的依赖目标集（也就是“foo.c bar.c”）</td></tr></tbody></table><p>​    这么做好处是当”%.o”有几百个时，只要用这种简答的”静态模式规则“就可以写完一堆规则，大大提高了效率。可以分析下面的案例</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment">#$(filter %.o,$(files))表示调用 Makefile 的 filter 函数，过滤“$filter”集，只要其中模式为&quot;%.o&quot;的内容</span></span><br><span class="line"><span class="comment"># 最终得到files中若干文件，如果需要得到何种文件只需要直接扩展即可</span></span><br><span class="line">files = foo.elc bar.o lose.o </span><br><span class="line"> </span><br><span class="line"><span class="variable">$(<span class="built_in">filter</span> %.o,<span class="variable">$(files)</span>)</span>: %.o: %.c </span><br><span class="line">gcc -c <span class="variable">$(CFLAGS)</span> <span class="variable">$&lt;</span> -o <span class="variable">$@</span> </span><br><span class="line"><span class="variable">$(<span class="built_in">filter</span> %.elc,<span class="variable">$(files)</span>)</span>: %.elc: %.el </span><br><span class="line">emacs -f batch-byte-compile <span class="variable">$&lt;</span></span><br></pre></td></tr></table></figure><h3 id="自动生成依赖性"><a href="#自动生成依赖性" class="headerlink" title="自动生成依赖性"></a>自动生成依赖性</h3><p>​    在makefile中依赖关系可能会包含一系列的头文件。当编译一个大型工程时就必须清楚知道哪些头文件，并且在增删文件时谨慎修改makefile文件，这样带来很多不便，此时就需要使用到C/C++编译器的一个功能：“-M”选项。</p><p>​    如果你使用 GNU 的 C/C++编译器，得用“**-MM**”参数，不然，“-M”参数会把一些标准库的头文件也包含进来。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">执行下面的命令：可以自动寻找源文件中包含的头文件，并生成一个依赖关系</span></span><br><span class="line">gcc -M main.c</span><br><span class="line"><span class="meta">#</span><span class="bash">输出如下：</span></span><br><span class="line">main.o : main.c defs.h</span><br></pre></td></tr></table></figure><p>​    GNU 组织建议把编译器为每一个源文件的自动生成的依赖关系放到一个文件中。为每一个“name.c”的文件都生成一个“name.d”的 Makefile 文件，**<u>[.d]文件中就存放对应[.c]文件的依赖关系</u>**。</p><p>​    于是可以写出[.c]文件和[.d]文件的依赖关系，并让 make 自动更新或生成[.d]文件，接着再包含在主makefile文件中，这样来自动化生成每个文件的依赖关系。下面给出一个模式规则用于产生[.d]文件。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 所有的[.d]文件依赖于[.c]文件</span></span><br><span class="line"><span class="comment"># “rm -f $@”的意思是删除所有的目标，也就是[.d]文件</span></span><br><span class="line"><span class="comment"># 第二行含义：为每个依赖文件“$&lt;”，也就是所有的.c文件生成依赖文件</span></span><br><span class="line">   <span class="comment"># “$@”表示模式“%.d”文件，“$$$$”意为一个随机编号，生成的文件有可能是“name.d.12345”</span></span><br><span class="line"><span class="comment"># 第三行含义：使用 sed 命令做了一个替换</span></span><br><span class="line">   <span class="comment"># rm -f $@.$$$$ 就是删除临时文件    </span></span><br><span class="line"><span class="section">%.d: %.c </span></span><br><span class="line">@set -e; rm -f <span class="variable">$@</span>; gcc -M <span class="variable">$(CPPFLAGS)</span> <span class="variable">$&lt;</span> &gt; <span class="variable">$@</span>.$$$$; \ </span><br><span class="line">sed &#x27;s,\(<span class="variable">$*</span>\)\.o[ :]*,\1.o <span class="variable">$@</span> : ,g&#x27; &lt; <span class="variable">$@</span>.$$$$ &gt; <span class="variable">$@</span>; rm -f <span class="variable">$@</span>.$$$$</span><br><span class="line"></span><br><span class="line"><span class="comment">#简而言之就是：在编译器生成的依赖关系中加入[.d]文件的依赖，即</span></span><br><span class="line">main.o : main.c defs.h </span><br><span class="line">转成： </span><br><span class="line">main.o main.d : main.c defs.h</span><br><span class="line"><span class="comment"># 最终[.d]文件也会自动更新了，并会自动生成了</span></span><br></pre></td></tr></table></figure><p>​    接着可以使用makefile中的“include”命令来引入别的makefile文件。</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># “$(sources:.c=.d)”中的“.c=.d”的意思是做一个替换</span></span><br><span class="line"><span class="comment"># 把变量$(sources)所有[.c]的字串都替换成[.d]</span></span><br><span class="line">sources = foo.c bar.c </span><br><span class="line"><span class="keyword">include</span> $(sources:.c=.d)</span><br></pre></td></tr></table></figure><h1 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h1><p><a href="https://blog.csdn.net/haoel/article/details/2886/">跟我一起写 Makefile</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;link rel=&quot;stylesheet&quot; class=&quot;aplayer-secondary-style-marker&quot; href=&quot;\css\APlayer.min.css&quot;&gt;&lt;script src=&quot;\js\APlayer.min.js&quot; class=&quot;aplayer-se</summary>
      
    
    
    
    
  </entry>
  
</feed>
